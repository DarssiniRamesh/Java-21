spring:
  application:
    name: java-21-backend

management:
  endpoints:
    web:
      exposure:
        include: health,info

# Do not hardcode server.port; preview maps automatically.

springdoc:
  api-docs:
    path: /openapi.json
  swagger-ui:
    path: /docs
