This directory holds the Maven Wrapper configuration and JAR.

At runtime, ./mvnw downloads the Maven Wrapper JAR (maven-wrapper.jar) using the URL specified in maven-wrapper.properties if it is not already present. In restricted environments, use ./mvn.sh to run the system 'mvn' command instead.
